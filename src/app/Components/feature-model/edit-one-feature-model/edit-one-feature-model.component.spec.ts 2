import { ComponentFixture, TestBed } from '@angular/core/testing';

import { EditOneFeatureModelComponent } from './edit-one-feature-model.component';

describe('EditOneFeatureModelComponent', () => {
  let component: EditOneFeatureModelComponent;
  let fixture: ComponentFixture<EditOneFeatureModelComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ EditOneFeatureModelComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(EditOneFeatureModelComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
